<%= form_with(model: leave, local: true) do |form| %>
  <% if leave.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leave.errors.count, "error") %> prohibited this leave from being saved:</h2>

      <ul>
      <% leave.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :leavedate %>
    <%= form.date_select :leavedate, id: :leave_leavedate %>
  </div>


  <div class="field">
    <%= form.label :user %>
    <%= form.collection_select(:user_id, User.all, :id, :username) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
