<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project %>
    <%= form.text_field :project, id: :task_project %>
  </div>

  <div class="field">
    <%= form.label :task_name %>
    <%= form.text_field :task_name, id: :task_task_name %>
  </div>

  <div class="field">
    <%= form.label :activity_name %>
    <%= form.text_field :activity_name, id: :task_activity_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
