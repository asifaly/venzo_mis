<p id="notice"><%= notice %></p>

<h1>Bankcontacts</h1>

<table>
  <thead>
    <tr>
      <th>Bank</th>
      <th>Contact</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bankcontacts.each do |bankcontact| %>
      <tr>
        <td><%= bankcontact.bank_id %></td>
        <td><%= bankcontact.contact_id %></td>
        <td><%= link_to 'Show', bankcontact %></td>
        <td><%= link_to 'Edit', edit_bankcontact_path(bankcontact) %></td>
        <td><%= link_to 'Destroy', bankcontact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bankcontact', new_bankcontact_path %>
